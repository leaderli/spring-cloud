eureka:
  client:
    service-url:
      defaultZone:  http://127.0.0.1:1111/eureka
spring:
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: config
      fail-fast: true
      label: master
